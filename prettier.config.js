/** @type {import('prettier').Config} */
const config = {
  singleQuote: false,
  arrowParens: "avoid",
  useTabs: false,
  trailingComma: "none",
  semi: true,
  printWidth: 80,
};

export default config;
