import { api } from "@stuff/api-client/react";

// export const useThreadsMoveMutation = () => {
//   const utils = api.useUtils();
//   return api.mail.threads.moveThreads.useMutation({
//     async onSettled(_, __, variables) {
//       await utils.mail.threads.getThreads.reset({
//         folderId: variables.folderId,
//       });
//     },
//   });
// };
