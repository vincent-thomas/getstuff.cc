export const getUserTable = (stage: string) => `${stage}-stuff-users`;
export const getCustomerTable = (stage: string) => `${stage}-stuff-customers`;
export const getDataTable = (stage: string) => `${stage}-stuff-data`;
