import * as Ariakit from "@ariakit/react";

const Root = Ariakit.DisclosureProvider;
const Content = Ariakit.DisclosureContent;
const Trigger = Ariakit.Disclosure;

export const Disclosure = { Root, Content, Trigger };
